import React from 'react';

const Transactions = () => {
  return (
    <div>
      <h2>Transactions</h2>
      {/* Liste transactions, détail, filtres, export, graphique revenus, recherche avancée */}
    </div>
  );
};

export default Transactions;
